# 私募产品分析静态页

- `index.html`：数据总览。
- `products.html`：产品列表、筛选及详情入口。
- `detail.html`：按产品按需加载详情。

产品目录和详情采用确定性 gzip 静态分片，由浏览器按需解压。请通过 HTTP/HTTPS 访问，不支持直接使用 `file://`。

产品基础目录保持公开来源；详情页可包含登录认证或合格投资者来源的业绩曲线及其衍生分析结果。发布包仅保留日期、收益值、指标结果和计算口径，不包含会话、凭据、原始响应或受限原始载荷。
